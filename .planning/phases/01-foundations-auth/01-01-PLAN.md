---
phase: 01-foundations-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - next.config.ts
  - tsconfig.json
  - .gitignore
  - .env.example
  - .env.local
  - components.json
  - src/app/layout.tsx
  - src/app/globals.css
  - src/app/page.tsx
  - src/components/ui/button.tsx
  - src/components/ui/card.tsx
  - src/components/ui/input.tsx
  - src/components/ui/label.tsx
  - src/components/ui/form.tsx
  - src/components/ui/alert.tsx
  - src/lib/utils.ts
  - vitest.config.ts
  - drizzle.config.ts
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05, GOAL-04]
tags: [scaffold, nextjs, tailwind, shadcn, supabase-setup, vitest, drizzle-config]

user_setup:
  - service: supabase
    why: "Required for NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, DATABASE_URL (pooler). Also required to whitelist redirect URL, enforce email verification, and set reset-link expiry to 15 min."
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase Dashboard → Project Settings → API → Project URL"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase Dashboard → Project Settings → API → anon public key"
      - name: DATABASE_URL
        source: "Supabase Dashboard → Project Settings → Database → Connection string → Transaction pooler (port 6543). Append ?pgbouncer=true&connection_limit=1 (Drizzle uses prepare:false which is compatible with this pooler mode)."
    dashboard_config:
      - task: "Whitelist local redirect URL"
        location: "Supabase Dashboard → Authentication → URL Configuration → Redirect URLs"
        value: "http://localhost:3000/**"
      - task: "Set Site URL"
        location: "Supabase Dashboard → Authentication → URL Configuration → Site URL"
        value: "http://localhost:3000"
      - task: "Enable email confirmations (required before login — D-16)"
        location: "Supabase Dashboard → Authentication → Providers → Email"
        value: "Confirm email = ON"
      - task: "Set password reset link expiry to 15 minutes (per D-17 / success criterion #3)"
        location: "Supabase Dashboard → Authentication → Email Templates / Rate Limits → Reset password token validity"
        value: "900 seconds (15 minutes)"

must_haves:
  truths:
    - "Next.js 16.2 app boots with `npm run dev` and renders a root page without errors"
    - "Tailwind v4 @theme tokens defined in globals.css (emerald primary, Geist font, zinc base)"
    - "shadcn components (button, card, input, label, form, alert) exist under src/components/ui/"
    - "Vitest runs with `npx vitest run` and reports 0 test files without errors (empty test run passes)"
    - "Env vars loaded: NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, DATABASE_URL all present in .env.local"
    - "drizzle.config.ts points at src/server/db/schema.ts and outputs to ./supabase/migrations"
  artifacts:
    - path: "package.json"
      provides: "Locked dependencies for the full stack"
      contains: "next.*16.2"
    - path: "src/app/globals.css"
      provides: "Tailwind v4 @theme with emerald primary, Geist font variables, zinc base"
      contains: "@theme"
    - path: "components.json"
      provides: "shadcn configuration (new-york style, zinc base color)"
      contains: "\"style\": \"new-york\""
    - path: "vitest.config.ts"
      provides: "Vitest runner with environment: 'node'"
      contains: "environment: 'node'"
    - path: "drizzle.config.ts"
      provides: "Drizzle kit config with out set to ./supabase/migrations"
      contains: "out: './supabase/migrations'"
    - path: ".env.local"
      provides: "Supabase URL + anon key + DATABASE_URL populated by user"
      contains: "NEXT_PUBLIC_SUPABASE_URL="
    - path: ".env.example"
      provides: "Committed template of required env vars with blank values"
      contains: "NEXT_PUBLIC_SUPABASE_URL="
    - path: ".gitignore"
      provides: "Ignores .env.local, node_modules, .next"
      contains: ".env.local"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "import './globals.css'"
      pattern: "import.*globals\\.css"
    - from: "src/app/layout.tsx"
      to: "next/font/google"
      via: "Geist + Geist_Mono variable fonts"
      pattern: "from ['\\\"]next/font/google['\\\"]"
    - from: "drizzle.config.ts"
      to: "src/server/db/schema.ts"
      via: "schema path (file created in Plan 03)"
      pattern: "schema: ['\\\"]\\./src/server/db/schema\\.ts['\\\"]"
---

<objective>
Scaffold Next.js 16.2 + React 19.2 + Tailwind v4 + shadcn/ui application with locked stack dependencies, brand tokens (emerald accent, Geist font, zinc base), Vitest + Drizzle config files, and environment variables wired to a user-provisioned Supabase project.

Purpose: Every subsequent plan (timezone tests, schema, auth) depends on this scaffold existing with version-pinned deps and the correct tool config paths.
Output: A runnable Next.js 16 app, installed shadcn components, Tailwind v4 CSS tokens matching the UI spec, a working `npx vitest run` command, a `drizzle.config.ts` that emits to `./supabase/migrations`, and a `.env.local` populated by the user with live Supabase credentials.
</objective>

<execution_context>
@/Users/rathtana.duong/gsd-tutorial/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rathtana.duong/gsd-tutorial/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@CLAUDE.md
@.planning/phases/01-foundations-auth/01-CONTEXT.md
@.planning/phases/01-foundations-auth/01-RESEARCH.md
@.planning/phases/01-foundations-auth/01-PATTERNS.md
@.planning/phases/01-foundations-auth/01-UI-SPEC.md
@.planning/phases/01-foundations-auth/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 16 + shadcn + stack install</name>
  <files>package.json, package-lock.json, next.config.ts, tsconfig.json, .gitignore, components.json, src/app/layout.tsx, src/app/globals.css, src/app/page.tsx, src/components/ui/button.tsx, src/components/ui/card.tsx, src/components/ui/input.tsx, src/components/ui/label.tsx, src/components/ui/form.tsx, src/components/ui/alert.tsx, src/lib/utils.ts</files>
  <read_first>
    - CLAUDE.md §Technology Stack and §Installation (version locks)
    - .planning/phases/01-foundations-auth/01-RESEARCH.md §Standard Stack Installation (lines 149-177) — exact install command sequence
    - .planning/phases/01-foundations-auth/01-PATTERNS.md §package.json (lines 634-649) — version locks list
    - .planning/phases/01-foundations-auth/01-UI-SPEC.md §shadcn Components Used (table) — 6 components to add
  </read_first>
  <action>
    Scaffold at the repo root (the repo IS the app). Use `--use-npm`. Do NOT create a subdirectory. Run the following exact commands in sequence (idempotent: if a file exists, prefer checking version rather than re-scaffolding — but expect all files to be created fresh on first run):

    1. `npx --yes create-next-app@16.2.4 . --typescript --tailwind --app --eslint --turbopack --src-dir --import-alias "@/*" --use-npm --yes` (overwrites permission required since `.planning/` and `CLAUDE.md` already exist — pass `--yes` to accept, and expect Next to refuse if `package.json` already exists; if refused, run instead: `npm init -y` then manually install `next@16.2.4 react@19.2.5 react-dom@19.2.5 typescript@5.9.3 @types/node @types/react @types/react-dom tailwindcss@4.2.2 @tailwindcss/postcss` and scaffold by hand per the "If create-next-app refuses" fallback below).

    **If create-next-app refuses (repo not empty):** Install dependencies manually:
    ```bash
    npm install next@16.2.4 react@19.2.5 react-dom@19.2.5
    npm install -D typescript@5.9.3 @types/node @types/react @types/react-dom eslint eslint-config-next@16.2.4 tailwindcss@4.2.2 @tailwindcss/postcss postcss
    ```
    Then hand-author `next.config.ts`, `tsconfig.json` (baseUrl=., paths={"@/*": ["./src/*"]}, include src/**/*), `postcss.config.mjs` (with `@tailwindcss/postcss` plugin), and `.gitignore` (standard Next.js template including `.env*.local`, `node_modules`, `.next`, `out`, `build`).

    2. Initialize shadcn with the locked preset (style `new-york`, base color `zinc`):
    ```bash
    npx --yes shadcn@3.5.0 init --yes --base-color zinc --css src/app/globals.css --style new-york
    ```
    This creates `components.json` with `"style": "new-york"`, `"baseColor": "zinc"`, `"cssVariables": true`, and configures Tailwind v4 CSS variables.

    3. Add the 6 shadcn components required by the UI-SPEC:
    ```bash
    npx --yes shadcn@3.5.0 add button card input label form alert --yes --overwrite
    ```

    4. Install Supabase SDK + SSR helper, Drizzle + postgres.js driver, React Hook Form + Zod bridge, @date-fns/tz, date-fns, lucide-react, and dev deps (Vitest, drizzle-kit, jsdom, @vitejs/plugin-react, prettier-plugin-tailwindcss). Use the EXACT versions from CLAUDE.md:
    ```bash
    npm install @supabase/supabase-js@2.103.3 @supabase/ssr@0.10.2 drizzle-orm@0.45.2 postgres@3.4.9 react-hook-form@7.72.1 zod@4.3.0 @hookform/resolvers@5.2.2 @date-fns/tz@1.4.1 date-fns@4.1.0 lucide-react@0.545.0
    npm install -D drizzle-kit@0.31.10 vitest@latest @vitejs/plugin-react@latest jsdom@latest prettier-plugin-tailwindcss@latest
    ```

    5. Edit `src/app/layout.tsx` to import Geist + Geist Mono via `next/font/google` (pattern below — COPY VERBATIM, replacing the default scaffold):
    ```typescript
    import type { Metadata } from "next"
    import { Geist, Geist_Mono } from "next/font/google"
    import "./globals.css"

    const geistSans = Geist({ variable: "--font-sans", subsets: ["latin"] })
    const geistMono = Geist_Mono({ variable: "--font-mono", subsets: ["latin"] })

    export const metadata: Metadata = {
      title: "Commitment Tracker",
      description: "Monthly commitment tracker — see every goal at a glance.",
    }

    export default function RootLayout({ children }: { children: React.ReactNode }) {
      return (
        <html lang="en" className={`${geistSans.variable} ${geistMono.variable}`}>
          <body className="bg-background text-foreground antialiased">{children}</body>
        </html>
      )
    }
    ```

    6. Overwrite `src/app/globals.css` with the full Tailwind v4 @theme block from UI-SPEC §Brand Tokens (lines 56-131 of 01-UI-SPEC.md). Copy ALL tokens verbatim including the `@import "tailwindcss"` directive on line 1, both the `@theme {}` block and the `@media (prefers-color-scheme: dark) { @theme {} }` override. Token values are OKLCH color functions — preserve exactly. Do NOT omit dark-mode overrides.

    7. Ensure `src/app/page.tsx` exists as a placeholder (since `/` is the post-login landing per UI-SPEC §Surface 6 but that's owned by Plan 05, ship a transitional placeholder that redirects to `/login` via a Server Component `redirect()` — the middleware in Plan 04 takes over routing logic, but this file must exist so the scaffold boots). Content:
    ```typescript
    import { redirect } from "next/navigation"
    export default function RootPage() {
      redirect("/login")
    }
    ```

    8. Create `src/lib/utils.ts` with shadcn's `cn` helper (shadcn CLI usually creates this; verify it exists and contains):
    ```typescript
    import { type ClassValue, clsx } from "clsx"
    import { twMerge } from "tailwind-merge"
    export function cn(...inputs: ClassValue[]) { return twMerge(clsx(inputs)) }
    ```
    If shadcn init did not install `clsx` or `tailwind-merge`, run `npm install clsx tailwind-merge`.

    9. Update `.gitignore` to ALSO include `.env.local`, `.env.*.local`, `supabase/.temp` (if present). Do NOT exclude `supabase/migrations` — migrations are committed.

    10. Run `npm run dev` briefly to confirm the app boots on port 3000, then Ctrl+C. Run `npx tsc --noEmit` to confirm no type errors.
  </action>
  <verify>
    <automated>test -f package.json && node -e "const p=require('./package.json'); const d={...p.dependencies,...p.devDependencies}; ['next','react','@supabase/ssr','drizzle-orm','@date-fns/tz','vitest','drizzle-kit'].forEach(k=>{ if(!d[k]) throw new Error('missing '+k); }); console.log('deps OK');" && test -f src/app/globals.css && grep -q "@theme" src/app/globals.css && grep -q "emerald" src/app/globals.css && test -f components.json && grep -q "new-york" components.json && test -f src/components/ui/button.tsx && test -f src/components/ui/form.tsx && test -f src/components/ui/alert.tsx && test -f src/app/layout.tsx && grep -q "Geist" src/app/layout.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` exists with `next` version matching `16.2.x`, `react` matching `19.2.x`, `tailwindcss` matching `4.2.x`, `@supabase/ssr` matching `0.10.x`, `drizzle-orm` matching `0.45.x`, `@date-fns/tz` matching `1.4.x`, `vitest` present in devDependencies
    - `components.json` exists and contains `"style": "new-york"` and `"baseColor": "zinc"`
    - Files exist at: `src/components/ui/{button,card,input,label,form,alert}.tsx`
    - `src/app/globals.css` contains the literal string `@theme` AND `emerald` AND `oklch(54% 0.155 161)` (emerald-600 primary)
    - `src/app/globals.css` contains `@media (prefers-color-scheme: dark)` block
    - `src/app/layout.tsx` contains `from "next/font/google"` AND `Geist` AND `Geist_Mono` AND `variable: "--font-sans"`
    - `src/lib/utils.ts` exports a `cn` function
    - `.gitignore` contains `.env.local`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Next.js scaffold boots; shadcn components installed; Tailwind v4 tokens from UI-SPEC present in globals.css; Geist fonts wired in root layout; TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Vitest config + drizzle-kit config + environment templates</name>
  <files>vitest.config.ts, drizzle.config.ts, .env.example, .env.local</files>
  <read_first>
    - .planning/phases/01-foundations-auth/01-RESEARCH.md §Code Examples → Vitest Config (lines 727-743)
    - .planning/phases/01-foundations-auth/01-RESEARCH.md §Code Examples → drizzle.config.ts (lines 826-839)
    - .planning/phases/01-foundations-auth/01-RESEARCH.md §Pitfall 4 (Drizzle output vs Supabase migrations path)
    - .planning/phases/01-foundations-auth/01-PATTERNS.md §drizzle.config.ts (lines 495-513) — CRITICAL RULE on `out` path
    - .planning/phases/01-foundations-auth/01-PATTERNS.md §vitest.config.ts (lines 517-538)
    - .planning/phases/01-foundations-auth/01-PATTERNS.md §.env.local (lines 654-665) — required keys
    - .planning/phases/01-foundations-auth/01-VALIDATION.md §Test Infrastructure
  </read_first>
  <action>
    Create the three config files plus env templates. All values are COPY-VERBATIM from the research/patterns docs.

    1. Create `vitest.config.ts` at repo root — copy verbatim from 01-PATTERNS.md lines 523-538:
    ```typescript
    import { defineConfig } from 'vitest/config'

    export default defineConfig({
      test: {
        environment: 'node',
        globals: true,
        include: ['tests/**/*.test.ts', 'src/**/*.test.ts'],
        coverage: {
          provider: 'v8',
          reporter: ['text', 'json'],
          include: ['src/lib/**/*.ts'],
        },
      },
    })
    ```
    Do NOT add `jsdom` as the environment — Phase 1 tests are pure functions only. Do NOT set `setupFiles` — not needed.

    2. Create `drizzle.config.ts` at repo root — copy verbatim from 01-PATTERNS.md lines 500-510:
    ```typescript
    import { defineConfig } from 'drizzle-kit'

    export default defineConfig({
      schema: './src/server/db/schema.ts',
      out: './supabase/migrations',
      dialect: 'postgresql',
      dbCredentials: {
        url: process.env.DATABASE_URL!,
      },
    })
    ```
    CRITICAL: `out: './supabase/migrations'` is REQUIRED (not `./drizzle`) so `supabase db push` picks up the SQL files. This is Pitfall 4.

    3. Create `.env.example` (committed to git) with exactly these three keys and blank values:
    ```
    # Supabase — Project Settings → API
    NEXT_PUBLIC_SUPABASE_URL=
    NEXT_PUBLIC_SUPABASE_ANON_KEY=

    # Postgres connection — Project Settings → Database → Transaction pooler (port 6543)
    # Append ?pgbouncer=true&connection_limit=1
    DATABASE_URL=
    ```

    4. Create `.env.local` (ignored by git) with the same three keys — LEAVE values blank. The user populates them in the checkpoint task below. Do NOT commit secrets; the file is generated empty here and populated by the user. Include a leading comment:
    ```
    # Phase 1 scaffolding — populate values in the checkpoint task.
    # Never commit this file.
    NEXT_PUBLIC_SUPABASE_URL=
    NEXT_PUBLIC_SUPABASE_ANON_KEY=
    DATABASE_URL=
    ```

    5. Create an empty folder structure so later plans do not need to mkdir: `mkdir -p src/server/db src/server/actions src/lib src/components/auth src/app/\(auth\) src/app/\(protected\) src/app/auth/callback tests supabase/migrations`. Do not create files inside beyond the scaffolded ones.
  </action>
  <verify>
    <automated>test -f vitest.config.ts && grep -q "environment: 'node'" vitest.config.ts && test -f drizzle.config.ts && grep -q "out: './supabase/migrations'" drizzle.config.ts && grep -q "schema: './src/server/db/schema.ts'" drizzle.config.ts && test -f .env.example && grep -q "^NEXT_PUBLIC_SUPABASE_URL=" .env.example && grep -q "^DATABASE_URL=" .env.example && test -f .env.local && grep -q "^NEXT_PUBLIC_SUPABASE_URL=" .env.local && test -d supabase/migrations && test -d src/server/db && test -d tests</automated>
  </verify>
  <acceptance_criteria>
    - `vitest.config.ts` contains literal string `environment: 'node'`
    - `vitest.config.ts` `include` array contains `'tests/**/*.test.ts'`
    - `drizzle.config.ts` contains literal string `out: './supabase/migrations'`
    - `drizzle.config.ts` contains literal string `schema: './src/server/db/schema.ts'`
    - `drizzle.config.ts` contains `dialect: 'postgresql'`
    - `.env.example` is committed (tracked by git) and contains the three keys with blank values
    - `.env.local` exists with the three keys, is listed in `.gitignore`, and is NOT tracked by git (`git check-ignore .env.local` exits 0)
    - Directories exist: `src/server/db/`, `src/lib/`, `src/components/auth/`, `src/app/(auth)/`, `src/app/(protected)/`, `src/app/auth/callback/`, `tests/`, `supabase/migrations/`
  </acceptance_criteria>
  <done>Vitest runs against `tests/**/*.test.ts` in Node environment; drizzle-kit emits to `./supabase/migrations`; env template is committed while secrets file is ignored; directory skeleton exists for downstream plans.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: CHECKPOINT — Create Supabase project + populate .env.local + dashboard config</name>
  <files>.env.local (populated by user; not modified by Claude)</files>
  <read_first>
    - .env.example (committed in Task 2 — shows required key names)
    - .env.local (currently has blank values — user fills them in)
    - .planning/phases/01-foundations-auth/01-CONTEXT.md D-16, D-17, D-18 (email-confirm ON, 15-min reset, cookie flags)
  </read_first>
  <what-built>
    The scaffold is in place: Next.js 16.2 + shadcn/ui + Tailwind v4 + stack dependencies installed. Vitest + drizzle-kit configs exist. `.env.local` exists with three EMPTY keys. Three pieces of human-only work remain: creating a Supabase project (requires a real account + email), copying secrets into `.env.local`, and toggling three dashboard settings (email-confirm ON, redirect URL allow-list, reset-link 15-min expiry). None of this can be done via CLI without an already-authenticated Supabase session.
  </what-built>
  <action>
    Human follows the step-by-step runbook in `<how-to-verify>` below — it is prescriptive (10 numbered steps) and exactly what must be performed manually in the Supabase dashboard and browser. Claude does NOT execute these steps; the resume-signal unblocks the next plan once the human completes them.
  </action>
  <how-to-verify>
    1. Visit https://supabase.com/dashboard → New Project → pick a name (e.g., `commitment-tracker-dev`) → choose a region close to you → set DB password (save it to your password manager) → Create.
    2. Wait ~2 minutes for provisioning.
    3. Navigate to **Project Settings → API**. Copy:
       - `Project URL` → paste as value for `NEXT_PUBLIC_SUPABASE_URL` in `.env.local`
       - `anon public` key → paste as value for `NEXT_PUBLIC_SUPABASE_ANON_KEY` in `.env.local`
    4. Navigate to **Project Settings → Database → Connection string → Transaction pooler (port 6543)**. Copy that connection string. Append `?pgbouncer=true&connection_limit=1` if not already present. Replace the placeholder `[YOUR-PASSWORD]` with your DB password. Paste as value for `DATABASE_URL` in `.env.local`.
    5. Navigate to **Authentication → URL Configuration**:
       - Set **Site URL** = `http://localhost:3000`
       - Add to **Redirect URLs** allow-list: `http://localhost:3000/**` (keeps callback + reset links working locally)
    6. Navigate to **Authentication → Providers → Email**:
       - Confirm **Confirm email** is set to **ON** (this enforces email verification before login — required by D-16 and success criterion #1)
    7. Navigate to **Authentication → Rate Limits** (or Email Templates → token expiry settings):
       - Set **Reset password token validity** = `900` seconds (15 minutes) per D-17 and success criterion #3. Note: Supabase's default is often 3600 — you must change it.
    8. Save all changes.
    9. Install Supabase CLI (one-time) if not already present: `brew install supabase/tap/supabase` (macOS) or `npm install -g supabase`. Then link this repo to the dev project: `supabase login` (browser OAuth) followed by `supabase link --project-ref <your-project-ref>`. The project-ref is the subdomain in your Supabase URL (`https://<project-ref>.supabase.co`).
    10. Back in the terminal, run `cat .env.local` and verify all three keys have values (URLs starting with `https://`, anon key ~200 chars JWT-shaped string, DATABASE_URL starting with `postgres://`). Test connectivity: `curl -s -H "apikey: $NEXT_PUBLIC_SUPABASE_ANON_KEY" "$NEXT_PUBLIC_SUPABASE_URL/rest/v1/" | head -c 200` — should return a JSON object describing the PostgREST API (not 401 or network error).

    Expected outcome: `.env.local` has three live values, Supabase dashboard has email-confirm ON + redirect URL whitelisted + reset expiry set to 15 min, Supabase CLI is linked. You will use the `DATABASE_URL` directly for migration apply in Plan 03.
  </how-to-verify>
  <verify>
    <automated>grep -E '^NEXT_PUBLIC_SUPABASE_URL=https://' .env.local && grep -E '^NEXT_PUBLIC_SUPABASE_ANON_KEY=ey' .env.local && grep -E '^DATABASE_URL=postgres' .env.local && (supabase projects list 2>/dev/null | head -5 || echo "supabase CLI not installed — manual link check required")</automated>
  </verify>
  <acceptance_criteria>
    - `.env.local` line `NEXT_PUBLIC_SUPABASE_URL=` starts with `https://` and ends with `.supabase.co`
    - `.env.local` line `NEXT_PUBLIC_SUPABASE_ANON_KEY=` starts with `ey` (JWT)
    - `.env.local` line `DATABASE_URL=` starts with `postgres://` or `postgresql://`
    - Supabase CLI is installed (`supabase --version` returns a version) AND this repo is linked (`supabase projects list` shows the linked project with a check mark, or `.supabase/config.json` exists with project_id)
    - User has confirmed in Supabase dashboard: Site URL = http://localhost:3000, Redirect URLs includes `http://localhost:3000/**`, Email provider Confirm email = ON, Reset password token validity = 900 seconds
  </acceptance_criteria>
  <done>Secrets populated in `.env.local`; Supabase dashboard enforces email-confirm + 15-min reset + allow-listed redirect URL; Supabase CLI linked to this project so Plan 03 can run `supabase db push --linked`.</done>
  <resume-signal>Type "approved" once all three values are in `.env.local` AND all three dashboard settings are saved AND `supabase link --project-ref <ref>` completed. If anything failed (project creation error, rate-limit setting not found, CLI install blocked, etc.), describe what blocked you and we'll troubleshoot.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer machine → Supabase dashboard | Manual human configuration of auth policies (email-confirm, link expiry) — relies on human not skipping steps |
| Local build process → npm registry | Package integrity for all locked versions |
| `.env.local` → git | Secrets must not leak via commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Information Disclosure | `.env.local` (secrets) | mitigate | `.gitignore` includes `.env.local` and `.env.*.local` (Task 2); `.env.example` committed with blank values only; checkpoint explicitly warns "never commit this file" |
| T-1-02 | Elevation of Privilege | Supabase service-role key | mitigate | Checkpoint ONLY captures `NEXT_PUBLIC_SUPABASE_ANON_KEY` (client-safe) + `DATABASE_URL` (server-only). Service-role key is NEVER stored in `.env.local` per RESEARCH.md §Security Domain |
| T-1-03 | Spoofing | Unverified-email account → session | mitigate | Checkpoint explicitly requires enabling "Confirm email = ON" in dashboard; D-16 enforces this at the login server-action layer in Plan 04 |
| T-1-04 | Tampering | Password reset link reuse/replay | mitigate | Checkpoint sets reset-token validity to 15 min (900s) per D-17; single-use is Supabase-enforced on `exchangeCodeForSession` |
| T-1-05 | Spoofing | Open-redirect via OAuth callback | mitigate | Checkpoint whitelists only `http://localhost:3000/**` in Supabase Redirect URLs; Supabase rejects callbacks to any other origin |
| T-1-06 | Tampering | Dependency substitution (supply chain) | accept | All versions pinned in package.json to exact minor versions per CLAUDE.md (npm view-verified 2026-04-17); npm audit will be run in Plan 05 verification |
</threat_model>

<verification>
**Automated:**
- `node -e "const p=require('./package.json'); ... }" ` — checks all critical deps present with major version match
- `grep` checks on every config file to verify COPY-VERBATIM content from research docs (emerald OKLCH tokens, `@theme`, `environment: 'node'`, `out: './supabase/migrations'`)
- `npx tsc --noEmit` — confirms zero TypeScript errors
- `git check-ignore .env.local` — confirms secrets file is ignored

**Manual (checkpoint task 3):**
- User confirms `cat .env.local` shows 3 populated values
- User confirms 3 Supabase dashboard toggles saved (email-confirm, redirect URL, 15-min reset expiry)
- User confirms `supabase link --project-ref <ref>` completed (prerequisite for Plan 03's `supabase db push --linked`)
- Optional smoke: `curl` against Supabase REST endpoint returns 200
</verification>

<success_criteria>
- [ ] `package.json` pins the exact versions from CLAUDE.md (Next 16.2.4, React 19.2.5, Tailwind 4.2.2, Drizzle 0.45.2, @supabase/ssr 0.10.2, @date-fns/tz 1.4.1, Vitest in devDeps)
- [ ] `src/app/globals.css` contains the full Tailwind v4 `@theme` block from UI-SPEC (emerald primary, Geist font vars, zinc base, dark-mode overrides)
- [ ] `components.json` indicates `new-york` style + `zinc` base color, and 6 shadcn components (button, card, input, label, form, alert) exist under `src/components/ui/`
- [ ] `vitest.config.ts` uses `environment: 'node'` and matches `tests/**/*.test.ts`
- [ ] `drizzle.config.ts` has `out: './supabase/migrations'` (Pitfall 4 compliance)
- [ ] `.env.local` exists, is git-ignored, and (after checkpoint) contains live Supabase URL + anon key + DATABASE_URL
- [ ] Supabase dashboard: Confirm email = ON, Redirect URL allow-lists `http://localhost:3000/**`, reset-token validity = 900s
- [ ] Supabase CLI installed + linked via `supabase link --project-ref <ref>` (required for Plan 03 `supabase db push --linked`)
- [ ] `npx tsc --noEmit` exits 0
- [ ] `npx vitest run` exits 0 (empty suite — no tests yet; Plan 02 adds them)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-auth/01-01-SUMMARY.md` with:
- List of installed dep versions (copy from `package.json`)
- Confirmed Supabase project URL/ref (so Plan 03 can run `supabase db push --linked` against the correct target)
- Any fallbacks used (e.g., if create-next-app refused, document manual scaffold)
- Reminder: `.env.local` is populated; DO NOT run `git add .env.local`.
</output>
