---
phase: 04-launch-polish
plan: "02"
type: execute
wave: 1
depends_on:
  - "04-01"
files_modified:
  - src/app/(protected)/dashboard/[month]/page.tsx
  - src/components/dashboard-shell.tsx
autonomous: true
requirements:
  - POLSH-01

must_haves:
  truths:
    - "At 375px, only icons are visible in the MonthNavigator right cluster (no text labels overflow)"
    - "Today button has aria-label='Return to this month' and title='Return to this month' with CalendarCheck icon"
    - "Log out button has aria-label='Log out' and title='Log out' with LogOut icon"
    - "New Goal button hides its label text on mobile via hidden md:inline span"
    - "At md: breakpoint (≥768px), all three buttons show icon + label side by side"
  artifacts:
    - path: "src/app/(protected)/dashboard/[month]/page.tsx"
      provides: "Rebuilt rightCluster ReactNode with responsive icon+label buttons"
      contains: "CalendarCheck"
    - path: "src/components/dashboard-shell.tsx"
      provides: "NewGoalButton with responsive hidden md:inline label"
      contains: "hidden md:inline"
  key_links:
    - from: "src/app/(protected)/dashboard/[month]/page.tsx"
      to: "src/components/month-navigator.tsx"
      via: "rightCluster ReactNode prop"
      pattern: "rightCluster"
    - from: "src/components/dashboard-shell.tsx"
      to: "src/components/ui/button.tsx"
      via: "Button size='icon' className merge via cn()"
      pattern: "Button"
---

<objective>
Make the MonthNavigator right cluster (Today, New Goal, Log out) responsive: icon-only on mobile (375px), icon+label at md: breakpoint. Per D-01 and D-02, all three buttons must have aria-label and title attributes. CalendarCheck replaces text "Today", LogOut icon replaces text "Log out", Plus icon already used for New Goal.

Purpose: POLSH-01 — dashboard usable at 375px without header overflow or two-row stacking.
Output: Rebuilt rightCluster in [month]/page.tsx + responsive NewGoalButton in dashboard-shell.tsx.
</objective>

<execution_context>
@/Users/rathtana/claude-projects/commitment-tracker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rathtana/claude-projects/commitment-tracker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-launch-polish/04-CONTEXT.md
@.planning/phases/04-launch-polish/04-PATTERNS.md
@.planning/phases/04-launch-polish/04-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rebuild rightCluster in [month]/page.tsx with responsive icon+label buttons</name>
  <files>src/app/(protected)/dashboard/[month]/page.tsx</files>

  <read_first>
    - src/app/(protected)/dashboard/[month]/page.tsx (full file — current rightCluster at lines 87-101, current imports at lines 1-30)
    - src/components/month-navigator.tsx (existing chevron aria-label pattern to match)
  </read_first>

  <action>
Read `src/app/(protected)/dashboard/[month]/page.tsx` fully before editing.

**Step 1 — Add lucide-react import.** There is currently no lucide-react import in this file. Add a new import line after the existing imports:
```tsx
import { CalendarCheck, LogOut } from "lucide-react"
```

**Step 2 — Rebuild the `rightCluster` constant (lines 87-101).** Replace the entire `rightCluster` block with:

```tsx
const rightCluster = (
  <>
    {!isCurrent && (
      <Button
        variant="outline"
        size="icon"
        aria-label="Return to this month"
        title="Return to this month"
        asChild
      >
        <Link href={`/dashboard/${currentSegment}`}>
          <CalendarCheck className="size-4" />
          <span className="hidden md:inline">Today</span>
        </Link>
      </Button>
    )}
    {status !== "past" && goals.length > 0 && (
      <NewGoalButton daysInMonthDefault={daysInMonth} />
    )}
    <form action={signOutAction}>
      <Button
        type="submit"
        variant="outline"
        size="icon"
        aria-label="Log out"
        title="Log out"
      >
        <LogOut className="size-4" />
        <span className="hidden md:inline">Log out</span>
      </Button>
    </form>
  </>
)
```

**Notes:**
- The Today button uses `variant="outline"` (not `variant="ghost"`) to match the existing right-cluster button style. The chevrons use "ghost" because they are navigation controls; the right cluster buttons are actions.
- `size="icon"` for Today and Log out is acceptable per UI-SPEC (navigation buttons, not progress-logging controls — 36px is fine for these).
- `asChild` on the Today Button causes it to render as the inner `<Link>`. The `CalendarCheck` icon and hidden label span are children of the Link.
- Log out remains a `<form>` wrapping a submit Button because it calls a server action (signOutAction). No `asChild` needed.
- The `NewGoalButton` call is unchanged — its responsive treatment happens in dashboard-shell.tsx (Task 2).
- Remove the old `size="sm"` `Button` elements that previously held text-only "Today" and "Log out" labels. They are replaced entirely by the new JSX above.
- Verify: no unused imports after the edit. The `Button` import and `Link` import were already present for the old code; they remain valid.
  </action>

  <verify>
    <automated>grep -n 'aria-label="Return to this month"' src/app/\(protected\)/dashboard/\[month\]/page.tsx && grep -n 'CalendarCheck' src/app/\(protected\)/dashboard/\[month\]/page.tsx && grep -n 'LogOut' src/app/\(protected\)/dashboard/\[month\]/page.tsx && grep -n 'aria-label="Log out"' src/app/\(protected\)/dashboard/\[month\]/page.tsx</automated>
  </verify>

  <acceptance_criteria>
    - src/app/(protected)/dashboard/[month]/page.tsx contains 'aria-label="Return to this month"'
    - src/app/(protected)/dashboard/[month]/page.tsx contains 'title="Return to this month"'
    - src/app/(protected)/dashboard/[month]/page.tsx contains 'CalendarCheck'
    - src/app/(protected)/dashboard/[month]/page.tsx contains 'aria-label="Log out"'
    - src/app/(protected)/dashboard/[month]/page.tsx contains 'title="Log out"'
    - src/app/(protected)/dashboard/[month]/page.tsx contains 'LogOut'
    - src/app/(protected)/dashboard/[month]/page.tsx contains 'hidden md:inline'
    - src/app/(protected)/dashboard/[month]/page.tsx does NOT contain 'size="sm">Today'
    - src/app/(protected)/dashboard/[month]/page.tsx does NOT contain 'size="sm">Log out'
    - npm run build exits 0 (TypeScript compiles)
  </acceptance_criteria>

  <done>rightCluster in [month]/page.tsx uses CalendarCheck + LogOut icons with aria-label, title, and hidden md:inline label spans. No TypeScript errors.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add responsive hidden md:inline label to NewGoalButton in dashboard-shell.tsx</name>
  <files>src/components/dashboard-shell.tsx</files>

  <read_first>
    - src/components/dashboard-shell.tsx (full file — NewGoalButton definition, current Plus + label render pattern)
  </read_first>

  <action>
Read `src/components/dashboard-shell.tsx` fully before editing. Locate the `NewGoalButton` component (approximately line 62-77 per PATTERNS.md).

**Current NewGoalButton render pattern (to replace):**
```tsx
<Button size={size} onClick={() => setOpen(true)} className={className}>
  <Plus className="mr-2 h-4 w-4" /> {label}
</Button>
```

**Target pattern — three changes:**
1. Remove `mr-2` from Plus icon className (the label is now inside a span that is hidden on mobile; when hidden, there is no adjacent text to add margin for).
2. Wrap `{label}` in `<span className="hidden md:inline">`.
3. Add `aria-label={label}` and `title={label}` to the Button for accessibility when label is hidden.

```tsx
<Button
  size={size}
  onClick={() => setOpen(true)}
  className={className}
  aria-label={label}
  title={label}
>
  <Plus className="h-4 w-4" />
  <span className="hidden md:inline">{label}</span>
</Button>
```

**Notes:**
- `mr-2` is removed because on mobile the span is `display: none` — there is no text to the right of the icon, so the margin adds unwanted gap. At `md:` the span is `display: inline` and the natural inline flow creates visual separation without margin.
- The `label` prop default is `'New goal'` (unchanged). The `aria-label={label}` and `title={label}` use the prop value so any future caller that passes a different label string gets correct accessibility text automatically.
- Do NOT change the `daysInMonthDefault` prop, the `size` prop, the `className` prop, or the `CreateGoalDialog` import. Only the Button's inner content and attribute additions change.
- After this change, the existing call site in `[month]/page.tsx` — `<NewGoalButton daysInMonthDefault={daysInMonth} />` — continues to work with no props change needed. The `size` prop defaults to `'default'`; the hidden label span handles responsive behavior regardless of size.
  </action>

  <verify>
    <automated>grep -n 'hidden md:inline' src/components/dashboard-shell.tsx && grep -n 'aria-label={label}' src/components/dashboard-shell.tsx && npm run test:e2e -- --grep "phase4" 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - src/components/dashboard-shell.tsx contains 'hidden md:inline'
    - src/components/dashboard-shell.tsx contains 'aria-label={label}'
    - src/components/dashboard-shell.tsx contains 'title={label}'
    - src/components/dashboard-shell.tsx does NOT contain 'mr-2 h-4 w-4' (mr-2 removed from Plus icon)
    - src/components/dashboard-shell.tsx contains 'h-4 w-4' (Plus icon className without mr-2)
    - npm run build exits 0
    - npm run test:e2e -- --grep "phase4" exits 0
  </acceptance_criteria>

  <done>NewGoalButton renders Plus icon + hidden md:inline label span. aria-label and title set from the label prop. mr-2 removed from Plus icon. All phase4 Playwright stubs still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → signOutAction | Log out form posts to server action; no new input surface introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Spoofing | MonthNavigator Log out button | accept | Button wraps existing signOutAction server action; @supabase/ssr handles session invalidation; no new attack surface |
| T-04-02-02 | Tampering | rightCluster Today link href | accept | href is server-computed from currentSegment (server-derived, not user input); no SSRF or open redirect |
| T-04-02-03 | Elevation of Privilege | Session cookie after signOut | mitigate | signOutAction calls supabase.auth.signOut() which invalidates the session token; @supabase/ssr middleware getUser() on next request returns null; user redirected to /login |
</threat_model>

<verification>
- `src/app/(protected)/dashboard/[month]/page.tsx` contains `aria-label="Return to this month"` and `CalendarCheck` and `LogOut`
- `src/components/dashboard-shell.tsx` contains `hidden md:inline` and `aria-label={label}`
- `npm run build` exits 0
- `npm run test:e2e -- --grep "phase4"` exits 0
</verification>

<success_criteria>
MonthNavigator right cluster buttons show icon-only at 375px and icon+label at md:. All three buttons have aria-label and title. No header overflow or row stacking at mobile viewport. TypeScript compiles clean.
</success_criteria>

<output>
After completion, create `.planning/phases/04-launch-polish/04-02-SUMMARY.md`
</output>
