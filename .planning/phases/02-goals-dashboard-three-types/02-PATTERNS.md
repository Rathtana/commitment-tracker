# Phase 2: Goals & Dashboard (Three Types) - Pattern Map

**Mapped:** 2026-04-19
**Files analyzed:** 27 (new) + 4 (modified)
**Analogs found:** 24 / 27 (strong in-repo analog) + 3 without close analog

> Pattern map for Phase 2 planner/executor. Every new file below points at a concrete
> analog in the existing codebase with file paths and line numbers. New files MUST
> copy these patterns rather than invent shapes. Three files (`progress-bar.tsx`,
> `habit-grid.tsx`, `dashboard-shell.tsx`) have no close analog — those must follow
> 02-RESEARCH.md Pattern 3 / Pattern 4 / Pattern 6 verbatim.

## File Classification

### New files

| New File | Role | Data Flow | Closest Analog | Match Quality |
|----------|------|-----------|----------------|---------------|
| `src/server/db/schema.ts` *(extend in place — 3 new tables + cols + CHECK)* | schema + RLS | CRUD | Same file (existing `goals` + `users` patterns, lines 14-95) | exact |
| `src/server/db/queries.ts` | data-access | request-response | `src/server/actions/auth.ts` direct-db block (lines 62-70) | role-match |
| `src/server/actions/goals.ts` | server action | CRUD | `src/server/actions/auth.ts` (lines 28-74) | exact |
| `src/server/actions/progress.ts` | server action | CRUD + transform | `src/server/actions/auth.ts` (lines 28-74) | exact |
| `src/server/services/goals.ts` *(optional — may inline)* | service | CRUD | `src/server/actions/auth.ts` signUp inline service pattern (lines 60-71) | role-match |
| `src/server/services/progress.ts` *(optional — may inline)* | service | CRUD + transform | `src/server/actions/auth.ts` signUp inline service pattern (lines 60-71) | role-match |
| `src/server/services/month.ts` *(optional)* | service | transform | `src/lib/time.ts` (lines 9-26) | role-match |
| `src/lib/schemas/goals.ts` | schema/validation | request-response | `src/lib/schemas/auth.ts` (lines 1-56) | exact |
| `src/lib/progress.ts` | utility (pure) | transform | `src/lib/time.ts` (lines 1-26) | exact |
| `src/app/(protected)/dashboard/page.tsx` | RSC route | request-response | `src/app/page.tsx` (lines 1-38) + `src/app/(auth)/login/page.tsx` (lines 1-34) | exact |
| `src/app/(protected)/layout.tsx` *(optional group layout)* | layout | request-response | `src/app/(auth)/layout.tsx` (lines 1-12) | exact |
| `src/components/progress-bar.tsx` | component | event-driven (animation) | **No repo analog** — follow 02-RESEARCH.md Pattern 3 (lines 463-515) verbatim | no analog |
| `src/components/pace-chip.tsx` | component (pure display) | transform | `src/components/ui/alert.tsx` `cva` variant pattern (lines 6-20) | role-match |
| `src/components/habit-grid.tsx` | component | event-driven | **No repo analog** — follow 02-RESEARCH.md Pattern 6 | no analog |
| `src/components/goal-card/index.tsx` | component (discriminator) | transform | `src/components/ui/button.tsx` `cva` variant pattern (lines 7-39) | role-match |
| `src/components/goal-card/count.tsx` | component | event-driven | `src/components/auth/login-form.tsx` (lines 1-117) — client component + action call + error surface | role-match |
| `src/components/goal-card/checklist.tsx` | component | event-driven | `src/components/auth/login-form.tsx` (lines 1-117) | role-match |
| `src/components/goal-card/habit.tsx` | component | event-driven | `src/components/auth/login-form.tsx` (lines 1-117) | role-match |
| `src/components/dashboard-shell.tsx` | component (state hoist) | event-driven | **No repo analog** — follow 02-RESEARCH.md Pattern 4 (lines 529-590) verbatim | no analog |
| `src/components/create-goal-dialog.tsx` | component (form) | request-response | `src/components/auth/signup-form.tsx` (lines 1-167) | exact |
| `src/components/delete-goal-dialog.tsx` | component (form) | request-response | `src/components/auth/reset-password-form.tsx` (lines 1-103) | role-match |
| `src/components/earlier-day-popover.tsx` | component (form) | request-response | `src/components/auth/update-password-form.tsx` (lines 1-103) | role-match |
| `src/components/empty-state.tsx` | component (RSC) | request-response | `src/app/page.tsx` (lines 6-38) | role-match |
| `src/components/goal-type-picker.tsx` | component | event-driven | `src/components/auth/login-form.tsx` client-state pattern | role-match |
| `supabase/migrations/0002_*.sql` *(auto-generated by drizzle-kit)* | migration | batch | `supabase/migrations/0000_initial_schema.sql` (lines 1-29) | exact |
| `supabase/migrations/0003_polymorphic_check.sql` *(manual CHECK)* | migration | batch | `supabase/migrations/0001_custom_constraints.sql` (lines 1-23) | exact |
| `tests/progress.test.ts` | test | — | `tests/time.test.ts` (lines 1-79) | exact |
| `tests/schemas.goals.test.ts` | test | — | `tests/schemas.auth.test.ts` (lines 1-159) | exact |
| `tests/actions.goals.test.ts` | test | — | `tests/actions.auth.test.ts` (lines 1-80) | exact |
| `tests/rls.goals.test.ts` *(extends existing)* | test | — | `tests/rls.test.ts` (lines 1-170) | exact |

### Modified files

| File | What changes | Analog to preserve |
|------|--------------|--------------------|
| `src/app/page.tsx` | Becomes `redirect('/dashboard')` OR hosts dashboard directly | Current shape at lines 1-38 |
| `src/app/layout.tsx` | Add `<Toaster />` from sonner INSIDE `<body>` OUTSIDE `{children}` (02-RESEARCH.md Pitfall 6) | Current shape at lines 13-19 |
| `src/middleware.ts` | No code change — `/dashboard` is not in `AUTH_ROUTES` so it's protected by default | Current shape at lines 11-18 |
| `src/app/globals.css` | Append `--color-warning-*` + `--color-success-*` tokens inside existing `@theme` block; do NOT touch `--spacing-*` (STATE.md Plan 01-05 lesson) | Existing `@theme` block at lines 4-35 and dark override at lines 38-66 |

---

## Pattern Assignments

### `src/server/db/schema.ts` (EXTEND in place) — schema + RLS, CRUD

**Analog:** `src/server/db/schema.ts` (existing `goals` table at lines 51-95 and `users` table at lines 14-45 of itself)

**Imports pattern to extend** (lines 1-12):

```typescript
import {
  pgTable,
  uuid,
  text,
  timestamp,
  date,
  pgEnum,
  foreignKey,
  pgPolicy,
} from "drizzle-orm/pg-core"
import { sql } from "drizzle-orm"
import { authenticatedRole, authUsers } from "drizzle-orm/supabase"
```

Phase 2 MUST add to these imports: `integer`, `boolean`, `primaryKey`. Keep `check` OUT unless used for a pure-column constraint (polymorphic CHECK goes in a manual migration per Phase 1 D-09 / 0001 pattern).

**Table + RLS policy pattern to copy verbatim for each new child table** (existing `goals` shape at lines 51-95):

```typescript
export const goals = pgTable(
  "goals",
  {
    id: uuid("id").primaryKey().defaultRandom().notNull(),
    userId: uuid("user_id").notNull(),
    // ... columns
    createdAt: timestamp("created_at", { withTimezone: true })
      .notNull()
      .defaultNow(),
    updatedAt: timestamp("updated_at", { withTimezone: true })
      .notNull()
      .defaultNow(),
  },
  (table) => [
    foreignKey({
      columns: [table.userId],
      foreignColumns: [users.id],
      name: "goals_user_id_fk",
    }).onDelete("cascade"),
    pgPolicy("goals-select-own", {
      for: "select",
      to: authenticatedRole,
      using: sql`user_id = auth.uid()`,
    }),
    pgPolicy("goals-insert-own", {
      for: "insert",
      to: authenticatedRole,
      withCheck: sql`user_id = auth.uid()`,
    }),
    pgPolicy("goals-update-own", {
      for: "update",
      to: authenticatedRole,
      using: sql`user_id = auth.uid()`,
      withCheck: sql`user_id = auth.uid()`,
    }),
    pgPolicy("goals-delete-own", {
      for: "delete",
      to: authenticatedRole,
      using: sql`user_id = auth.uid()`,
    }),
  ],
)
```

**Apply to:** `tasks`, `habit_check_ins`, `progress_entries` — each gets four policies (select / insert / update / delete). Because children have NO `user_id` column (D-08), the policy predicate MUST be a `goal_id IN (SELECT id FROM goals WHERE user_id = auth.uid())` subquery instead of `user_id = auth.uid()`. Use this shape for children:

```typescript
pgPolicy("tasks-select-own", {
  for: "select",
  to: authenticatedRole,
  using: sql`goal_id IN (SELECT id FROM goals WHERE user_id = auth.uid())`,
}),
```

**Composite PK pattern for `habit_check_ins`** (D-02) — use `primaryKey({ columns: [...] })`:

```typescript
(table) => [
  primaryKey({ columns: [table.goalId, table.checkInDate] }),
  foreignKey({ columns: [table.goalId], foreignColumns: [goals.id], name: "habit_check_ins_goal_id_fk" }).onDelete("cascade"),
  // ... policies
]
```

**What CANNOT go in schema.ts** (must go in a manual migration — see next entry): the polymorphic-validity CHECK on `goals` (D-04), non-negativity CHECKs (`current_count >= 0`, etc.). Drizzle pg-core does not express multi-column CHECKs reliably — migration 0001 pattern applies (line 1-6 of `supabase/migrations/0001_custom_constraints.sql`).

---

### `supabase/migrations/0003_polymorphic_check.sql` — migration, batch

**Analog:** `supabase/migrations/0001_custom_constraints.sql` (lines 1-6)

**Pattern to copy** (lines 1-6):

```sql
-- CHECK constraint: goals.month must be first-of-month (GOAL-04, CONTEXT.md D-05)
-- Cannot be expressed in Drizzle pgTable; added manually per RESEARCH.md Pitfall 3.
ALTER TABLE public.goals
  ADD CONSTRAINT month_is_first_of_month
  CHECK (EXTRACT(DAY FROM month) = 1);
```

**Apply to Phase 2:** Add the polymorphic-validity CHECK from CONTEXT.md D-04 + non-negativity CHECKs. File header MUST include a `-- source: D-04 + D-03` breadcrumb. Commit this file manually AFTER `drizzle-kit generate` produces 0002 for the table shape.

---

### `src/server/actions/goals.ts` AND `src/server/actions/progress.ts` — server action, CRUD

**Analog:** `src/server/actions/auth.ts` (lines 1-74 — the full `signUpAction` flow)

**Imports pattern** (lines 1-17):

```typescript
"use server"

import { redirect } from "next/navigation"
import { eq } from "drizzle-orm"
import { getSupabaseServerClient } from "@/lib/supabase/server"
import { db } from "@/server/db"
import { users } from "@/server/db/schema"
import {
  signUpSchema,
  signInSchema,
  // ...
  type SignUpInput,
} from "@/lib/schemas/auth"
```

**Apply to Phase 2:** Replace `auth` imports with `goals` equivalents; add `revalidatePath` import for post-mutation revalidation (02-RESEARCH.md Pattern 5); import `tasks`, `habitCheckIns`, `progressEntries`, `goals` schema objects.

**ActionResult type pattern** (line 19):

```typescript
type ActionResult = { error?: string }
```

**Apply to Phase 2:** Expand to the richer shape from 02-RESEARCH.md Pattern 5:

```typescript
type ActionResult<T = void> = { ok: true; data: T } | { ok: false; error: string }
```

This shape is needed so the optimistic client in `dashboard-shell.tsx` can type-narrow on `result.ok` (02-RESEARCH.md Pattern 4 expects `result.ok`).

**Full server-action body pattern to copy** (lines 28-74, `signUpAction`):

```typescript
export async function signUpAction(
  input: SignUpInput,
): Promise<ActionResult> {
  const parsed = signUpSchema.safeParse(input)
  if (!parsed.success) {
    return { error: "Invalid input. Please check the form." }
  }
  const { email, password, timezone } = parsed.data

  const supabase = await getSupabaseServerClient()

  const { data, error } = await supabase.auth.signUp({
    email,
    password,
    options: {
      emailRedirectTo: `${siteUrl()}/auth/callback?next=/auth/verify`,
    },
  })
  if (error) {
    // ... handle error
    return { error: "Something went wrong. Please try again." }
  }

  // Two-step write (comment block lines 57-59 documents the ordering contract)
  const userId = data.user?.id
  if (userId && timezone && timezone !== "UTC") {
    try {
      await db
        .update(users)
        .set({ timezone, updatedAt: new Date() })
        .where(eq(users.id, userId))
    } catch {
      // Non-fatal: ...
    }
  }

  return {}
}
```

**Apply to Phase 2 (per action):**
1. `safeParse` input with the Zod schema from `src/lib/schemas/goals.ts` — return `{ok:false, error:'Invalid input.'}` on fail
2. Call `getSupabaseServerClient()` and `supabase.auth.getUser()` — return `{ok:false, error:'Not authenticated.'}` on missing user
3. Service call (or inline the Drizzle mutation) — wrap multi-write operations in `db.transaction(async tx => { ... })` so D-06 / D-33 atomicity holds
4. After mutation: `revalidatePath('/dashboard')` (02-RESEARCH.md Pattern 5)
5. Return `{ok:true, data: <undoId or void>}`

**Ownership assertion** (Phase 1 pattern — service asserts, RLS defends): before any mutation on a child row, the action/service MUST verify `goal.user_id === user.id`. Do not rely solely on RLS — that's defense-in-depth (02-CONTEXT.md code-context block line 203).

**Action list per file:**
- `goals.ts`: `createGoalAction`, `updateGoalAction`, `deleteGoalAction`
- `progress.ts`: `incrementCountAction`, `toggleTaskAction`, `upsertHabitCheckInAction`, `backfillCountAction`, `undoLastMutationAction`

---

### `src/lib/schemas/goals.ts` — schema/validation, request-response

**Analog:** `src/lib/schemas/auth.ts` (lines 1-56)

**File-header docstring pattern** (lines 1-11):

```typescript
import { z } from "zod"

/**
 * Canonical Zod schemas for every auth surface.
 * Imported by:
 *   - Server actions (src/server/actions/auth.ts) for server-side re-validation
 *   - Client forms (Plan 01-05) via zodResolver for client-side validation
 *
 * Error copy is verbatim from 01-UI-SPEC.md per-surface validation rules —
 * changing a string here changes every form that imports it.
 */
```

**Apply to Phase 2:** Reuse the docstring verbatim, swapping `auth` → `goals` and `01-UI-SPEC.md` → `02-UI-SPEC.md §Copywriting Contract` (which locks error strings for goal forms).

**Reusable field pattern** (lines 13-19):

```typescript
export const emailField = z
  .string()
  .email("Enter a valid email address")

export const passwordField = z
  .string()
  .min(8, "Password must be at least 8 characters")
```

**Apply to Phase 2:** Factor shared fields — `titleField`, `targetCountField` (positive int), `targetDaysField` (1..31 int), `notesField` (optional string).

**Discriminated-union (NEW shape, not in auth.ts) — per 02-RESEARCH.md D-20 / Pattern:**

The auth schemas use plain objects. Phase 2 is FIRST USE of `z.discriminatedUnion("type", ...)` in the repo. Shape:

```typescript
const baseCreateGoal = z.object({
  title: titleField,
  notes: notesField,
  // month is server-derived from monthBucket(new Date(), userTz); NOT in form input (D-17)
})

export const createCountGoalSchema = baseCreateGoal.extend({
  type: z.literal("count"),
  targetCount: targetCountField,
})
export const createChecklistGoalSchema = baseCreateGoal.extend({
  type: z.literal("checklist"),
  tasks: z.array(z.object({ label: z.string().min(1), position: z.number().int().nonnegative() })).min(1, "Add at least one task."),
})
export const createHabitGoalSchema = baseCreateGoal.extend({
  type: z.literal("habit"),
  targetDays: targetDaysField,
})

export const createGoalSchema = z.discriminatedUnion("type", [
  createCountGoalSchema,
  createChecklistGoalSchema,
  createHabitGoalSchema,
])
export type CreateGoalInput = z.infer<typeof createGoalSchema>
```

**Type export pattern** (lines 52-55):

```typescript
export type SignUpInput = z.infer<typeof signUpSchema>
```

**Apply to Phase 2:** Export `CreateGoalInput`, `UpdateGoalInput`, `IncrementCountInput`, `ToggleTaskInput`, `UpsertHabitCheckInInput`, `BackfillCountInput`, `UndoLastMutationInput`.

**Zod 4 input-vs-output caveat** (documented in `src/components/auth/signup-form.tsx` lines 24-31): if any schema uses `.default(...)` or otherwise diverges input from output, RHF typing needs `z.input<typeof schema>` — not `z.infer`. Planner/executor MUST call this out where it applies (e.g., optional notes field or server-derived month).

---

### `src/lib/progress.ts` — utility (pure), transform

**Analog:** `src/lib/time.ts` (lines 1-26)

**Full file as template** (lines 1-26):

```typescript
import { TZDate } from '@date-fns/tz'
import { startOfMonth, format } from 'date-fns'

/**
 * Returns the user's local date as 'YYYY-MM-DD'.
 * @param now    - A UTC Date (e.g., new Date() on the server)
 * @param userTz - IANA timezone string from public.users.timezone
 */
export function today(now: Date, userTz: string): string {
  const local = new TZDate(now.getTime(), userTz)
  return format(local, 'yyyy-MM-dd')
}

/**
 * Returns a Date representing the first day of the user's local month.
 * Used for goals.month writes and "current month" queries.
 * @param now    - A UTC Date
 * @param userTz - IANA timezone string
 */
export function monthBucket(now: Date, userTz: string): Date {
  const local = new TZDate(now.getTime(), userTz)
  const first = startOfMonth(local)
  return new Date(format(first, 'yyyy-MM-dd') + 'T00:00:00.000Z')
}
```

**Apply to Phase 2:**
- Pure functions only — no DB access, no `new Date()` inside; take `now: Date` and `userTz: string` as parameters so Vitest can inject deterministic fixtures (matches `tests/time.test.ts` pattern verbatim)
- Use `TZDate` from `@date-fns/tz` for timezone-aware day/month math (import `today` / `monthBucket` from `./time` to avoid duplicating the wrapper)
- Export signature per CONTEXT.md D-10 exactly:

```typescript
export function computeProgress(
  goal: Goal,
  now: Date,
  userTz: string,
): {
  percent: number
  raw: { done: number; total: number }
  expected: number
  pace: 'on-pace' | 'behind' | 'ahead' | 'warming-up'
  paceDelta: number
}
```

- Per-type branch per D-11 and D-12:
  - `count`: `percent = currentCount / targetCount`
  - `checklist`: `percent = doneTasks / totalTasks`; `pace` is forced to `'on-pace'` (D-12 — no time axis)
  - `habit`: `percent = uniqueCheckInDates / targetDays`
- Early-month guard: `if (daysElapsed < 5) return {..., pace: 'warming-up', paceDelta: 0}` (D-13)

---

### `tests/progress.test.ts` — test

**Analog:** `tests/time.test.ts` (lines 1-79)

**Imports + describe pattern** (lines 1-3):

```typescript
import { describe, it, expect, afterEach, vi } from 'vitest'
import { today, monthBucket } from '../src/lib/time'

describe('today()', () => {
  afterEach(() => vi.useRealTimers())

  it('UTC-8: 11:30 PM on March 31 → March 31', () => {
    const now = new Date('2026-04-01T06:30:00.000Z')
    expect(today(now, 'America/Los_Angeles')).toBe('2026-03-31')
  })
```

**Apply to Phase 2:** Import `computeProgress` from `../src/lib/progress`. One `describe` per goal type (`count`, `checklist`, `habit`) plus one for `pace`. Per CONTEXT.md D-10 and 02-RESEARCH.md §Progress Validation: cover day 1, day 5 (early-month threshold), mid-month, last-day, DST spring-forward, leap year Feb 29, and all three types × (empty / on-pace / behind / ahead / complete).

**Fixture timestamp style:** Always `new Date('YYYY-MM-DDTHH:mm:ss.sssZ')` (UTC ISO string) to avoid host-TZ drift.

---

### `tests/schemas.goals.test.ts` — test

**Analog:** `tests/schemas.auth.test.ts` (lines 1-159)

**Imports + describe pattern** (lines 1-8):

```typescript
import { describe, it, expect } from "vitest"
import {
  signUpSchema,
  signInSchema,
  resetRequestSchema,
  updatePasswordSchema,
} from "../src/lib/schemas/auth"

describe("signUpSchema", () => {
  it("accepts valid signup input", () => {
    const result = signUpSchema.safeParse({ ... })
    expect(result.success).toBe(true)
  })
```

**UI-SPEC-copy-locking pattern** (lines 32-43):

```typescript
it("rejects invalid email with exact UI-SPEC copy", () => {
  const result = signUpSchema.safeParse({ email: "not-an-email", ... })
  expect(result.success).toBe(false)
  if (!result.success) {
    const msg = result.error.issues.find((i) => i.path[0] === "email")?.message
    expect(msg).toBe("Enter a valid email address")
  }
})
```

**Apply to Phase 2:** Lock every error string from 02-UI-SPEC.md §Copywriting Contract → Error/Rollback / Title field placeholder / Target field placeholder with identical `expect(msg).toBe(...)` assertions. ADD: a discriminated-union-specific test that feeds `{type: 'count', tasks: [...]}` (impossible combination) and asserts `result.success === false` — proves the discriminated union rejects cross-type pollution.

---

### `tests/actions.goals.test.ts` — test

**Analog:** `tests/actions.auth.test.ts` (lines 1-80)

**`next/headers` + `next/navigation` mock pattern** (lines 18-33):

```typescript
// next/headers cookies() throws outside a request scope. Mock it so the
// module under test can be imported and the safeParse-reject branches execute
// before we ever call the real supabase client.
vi.mock("next/headers", () => ({
  cookies: async () => ({
    getAll: () => [],
    set: () => {},
  }),
}))

// Stub redirect() so it's observable.
vi.mock("next/navigation", () => ({
  redirect: vi.fn((path: string) => {
    throw new Error(`REDIRECT:${path}`)
  }),
}))
```

**Shape + rejection test pattern** (lines 34-55):

```typescript
describe("src/server/actions/auth.ts module exports", () => {
  it("exports all five server actions", async () => {
    const mod = await import("../src/server/actions/auth")
    expect(typeof mod.signUpAction).toBe("function")
    // ...
  })
})

describe("signUpAction Zod rejection", () => {
  it("rejects malformed input before touching Supabase", async () => {
    const { signUpAction } = await import("../src/server/actions/auth")
    const result = await signUpAction({ email: "not-an-email", ... })
    expect(result.error).toBeDefined()
  })
})
```

**Apply to Phase 2:** One describe block per action (`createGoalAction`, `incrementCountAction`, etc.) verifying the action rejects with `{ok:false, error:...}` BEFORE hitting Supabase. Also add `revalidatePath` mock for progress actions:

```typescript
vi.mock("next/cache", () => ({ revalidatePath: vi.fn() }))
```

---

### `tests/rls.goals.test.ts` (or extend `tests/rls.test.ts`) — test

**Analog:** `tests/rls.test.ts` (lines 1-170)

**`asUser()` helper pattern** (lines 23-38):

```typescript
async function asUser<T>(
  userId: string,
  fn: (tx: postgres.TransactionSql) => Promise<T>,
): Promise<T> {
  return sql.begin(async (tx) => {
    await tx.unsafe(`SET LOCAL role = authenticated`)
    await tx.unsafe(
      `SET LOCAL request.jwt.claims = '${JSON.stringify({
        sub: userId,
        role: 'authenticated',
      })}'`,
    )
    return fn(tx)
  }) as Promise<T>
}
```

**Cross-user isolation assertion pattern** (lines 117-157):

```typescript
it('user A UPDATE B goal → 0 rows affected (RLS filters UPDATE)', async () => {
  const result = await asUser(USER_A, (tx) =>
    tx`UPDATE public.goals SET title = 'hacked' WHERE id = ${goalB_id}`,
  )
  expect(result.count).toBe(0)
})

it('user A INSERT with user_id = B → rejected by WITH CHECK', async () => {
  await expect(
    asUser(USER_A, (tx) =>
      tx`INSERT INTO public.goals (id, user_id, month, type, title) VALUES (...)`,
    ),
  ).rejects.toThrow(/row-level security/i)
})
```

**Apply to Phase 2:** Add 4 describe blocks — `tasks`, `habit_check_ins`, `progress_entries`, and `goals polymorphic CHECK`. For each child, test: SELECT (isolation), INSERT (via a goal owned by B → expect rejected), UPDATE / DELETE (0 rows). Additionally — test the polymorphic CHECK rejects impossible states:

```typescript
it('goals CHECK rejects (type=count, target_count=NULL)', async () => {
  await expect(
    asUser(USER_A, (tx) => tx`
      INSERT INTO public.goals (id, user_id, month, type, title, target_count)
      VALUES (${randomUUID()}, ${USER_A}, '2026-04-01', 'count', 'bad', NULL)
    `),
  ).rejects.toThrow(/check/i)
})
```

---

### `src/app/(protected)/dashboard/page.tsx` — RSC route, request-response

**Analog:** `src/app/page.tsx` (lines 1-38) + `src/app/(auth)/login/page.tsx` (lines 1-34)

**Auth-gate pattern at top of RSC** (`src/app/page.tsx` lines 1-16):

```typescript
import { redirect } from "next/navigation"
import { getSupabaseServerClient } from "@/lib/supabase/server"
import { signOutAction } from "@/server/actions/auth"
import { Button } from "@/components/ui/button"

export default async function LandingPage() {
  const supabase = await getSupabaseServerClient()
  const {
    data: { user },
  } = await supabase.auth.getUser()

  // Defense in depth: middleware should have redirected already, but if
  // somehow an unauthenticated request lands here, redirect to login.
  if (!user) {
    redirect("/login")
  }
```

**Apply to Phase 2:** Copy lines 1-16 verbatim. Then:
1. Fetch `user.timezone` from `public.users` via Drizzle (the authenticated `db` client — RLS enforces ownership automatically)
2. Call `monthBucket(new Date(), user.timezone)` to resolve the current month (D-17)
3. Call `getMonthDashboard(userId, month)` from `src/server/db/queries.ts` — one round-trip returning goals + children (PITFALLS §3 30-line budget)
4. Pass `initialGoals`, `userTz`, `now` as props into `<DashboardShell />` (a client component)
5. Ensure `Toaster` is mounted (in `app/layout.tsx`, not here — 02-RESEARCH.md Pitfall 6)

**Route group + middleware contract** (from `src/middleware.ts` lines 8-18 + comment lines 4-10): `src/app/(protected)/dashboard/page.tsx` resolves to request path `/dashboard` (parens stripped). `/dashboard` is NOT in `AUTH_ROUTES` — middleware therefore redirects unauthenticated users to `/login` by default. No middleware change needed.

**Async searchParams pattern** (`src/app/(auth)/login/page.tsx` lines 5-12):

```typescript
type SearchParams = Promise<{ reset?: string }>

export default async function LoginPage({
  searchParams,
}: {
  searchParams: SearchParams
}) {
  const { reset } = await searchParams
```

Not needed for Phase 2 dashboard (no query params), but applies to future `/dashboard/[month]` — noted for Phase 3.

---

### `src/app/(protected)/layout.tsx` — layout (optional)

**Analog:** `src/app/(auth)/layout.tsx` (lines 1-12)

**Full file as template:**

```typescript
export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return (
    <main className="flex min-h-screen items-center justify-center px-4 py-16">
      <div className="w-full max-w-sm">
        <p className="text-center text-xs text-muted-foreground mb-4">
          Commitment Tracker
        </p>
        {children}
      </div>
    </main>
  )
}
```

**Apply to Phase 2:** Wrap dashboard in `<main className="max-w-[720px] mx-auto px-4 py-8 md:py-12 flex flex-col gap-4">` per 02-UI-SPEC.md §Layout (lines 660-678). Do NOT include "Commitment Tracker" header again if the landing stub already has one — planner decides.

---

### `src/components/progress-bar.tsx` — component, event-driven (animation)

**Analog:** NONE in repo. Follow 02-RESEARCH.md Pattern 3 verbatim.

**Canonical code** (02-RESEARCH.md lines 463-515):

```typescript
'use client'

import { motion, useReducedMotion } from 'motion/react'
import { cn } from '@/lib/utils'

interface ProgressBarProps {
  percent: number          // 0..1
  expected?: number        // 0..1 for the pace tick (optional)
  className?: string
  ariaLabel: string        // e.g. "Read 5 books: 3 of 5 (60%)"
}

export function ProgressBar({ percent, expected, className, ariaLabel }: ProgressBarProps) {
  const reduce = useReducedMotion()
  const clamped = Math.max(0, Math.min(1, percent))

  return (
    <div
      role="progressbar"
      aria-valuemin={0}
      aria-valuemax={100}
      aria-valuenow={Math.round(clamped * 100)}
      aria-label={ariaLabel}
      className={cn('relative h-2 w-full overflow-hidden rounded-full bg-muted', className)}
    >
      <motion.div
        className="absolute inset-y-0 left-0 w-full origin-left rounded-full bg-primary"
        style={{ willChange: 'transform' }}
        initial={false}
        animate={{ scaleX: clamped }}
        transition={
          reduce
            ? { duration: 0 }
            : { type: 'spring', stiffness: 140, damping: 22, mass: 0.6 }
        }
      />
      {typeof expected === 'number' && (
        <div
          aria-hidden
          className="pointer-events-none absolute top-0 h-full w-px bg-foreground/40"
          style={{ left: `${Math.max(0, Math.min(1, expected)) * 100}%` }}
        />
      )}
    </div>
  )
}
```

**Critical invariants** (MUST NOT be altered):
- `scaleX` (not `width`) + `transform-origin: left` + `will-change: transform` — CLS safety (D-23; addresses STATE.md research flag)
- `initial={false}` — prevents re-animation from 0 on re-render
- `useReducedMotion()` branch — accessibility
- Fill element is a sibling `<motion.div>` — NEVER a Radix `Progress.Indicator`
- `cn(...)` from `@/lib/utils` — use the existing util; same import pattern as every other component in the repo

**Token usage:** `bg-muted` (rail), `bg-primary` (fill), `bg-foreground/40` (tick). No new tokens needed for the bar itself; pace-chip tokens go in `pace-chip.tsx`.

---

### `src/components/pace-chip.tsx` — component, transform (pure display)

**Analog:** `src/components/ui/alert.tsx` (lines 6-20) — `cva` variant pattern

**cva variant pattern to follow** (`src/components/ui/alert.tsx` lines 6-20):

```typescript
const alertVariants = cva(
  "relative grid w-full grid-cols-[0_1fr] items-start gap-y-0.5 rounded-lg border px-4 py-3 text-sm ...",
  {
    variants: {
      variant: {
        default: "bg-card text-card-foreground",
        destructive: "bg-card text-destructive ...",
      },
    },
    defaultVariants: { variant: "default" },
  }
)
```

**Apply to Phase 2:**

```typescript
const paceChipVariants = cva(
  "inline-flex items-center rounded-md px-2 py-0.5 text-xs font-medium tabular-nums",
  {
    variants: {
      pace: {
        'on-pace': "text-muted-foreground",
        behind:    "bg-warning-muted text-warning-foreground",
        ahead:     "bg-success-muted text-success-foreground",
      },
    },
    defaultVariants: { pace: 'on-pace' },
  }
)
```

**Warming-up branch:** Return `null` instead of a styled chip when `pace === 'warming-up'` (D-13 + 02-UI-SPEC.md §Pace Chip Copy "hidden" row). Checklist: also return `null` — the UI-SPEC prefers suppressing the chip over showing "on pace" with no semantic.

---

### `src/components/habit-grid.tsx` — component, event-driven

**Analog:** NONE in repo. Follow 02-RESEARCH.md Pattern 6.

**Grid structure contract (from 02-UI-SPEC.md §Habit Card lines 516-545):**
- Weekday header: `grid grid-cols-7 gap-1` with `"S M T W T F S"` in `text-xs text-muted-foreground text-center`
- Grid body: `grid grid-cols-7 gap-1`
- Each cell is a `<button>` (not `<div>` — D-40 accessibility)
- Visual `h-9 w-9` (36px) wrapped in `min-h-11 min-w-11` touch target (POLSH-01)
- State classes (per 02-UI-SPEC.md lines 534-540):
  - Hit: `bg-primary text-primary-foreground`
  - Miss (past): `bg-muted text-muted-foreground`
  - Today (either): `ring-2 ring-ring ring-offset-2 ring-offset-card`
  - Future: `opacity-50 cursor-not-allowed` + `disabled` attribute

**Date-fns helper pattern** (02-RESEARCH.md Pattern 6):

```typescript
import { eachDayOfInterval, startOfMonth, endOfMonth, getDay, format } from 'date-fns'
import { TZDate } from '@date-fns/tz'

const days = eachDayOfInterval({ start: startOfMonth(month), end: endOfMonth(month) })
const leadingBlanks = getDay(days[0])  // 0=Sun..6=Sat
// prepend `leadingBlanks` empty <div> spacers; 42 cells total; trailing <div> to fill
```

**Roving tabindex** (02-UI-SPEC.md §Keyboard Navigation line 617): only the focused cell has `tabIndex={0}`; others `tabIndex={-1}`. Arrow keys move focus; Enter/Space toggles.

**aria-label pattern** (D-40 + 02-UI-SPEC.md line 543):

```typescript
aria-label={`${format(date, 'MMMM d')} — ${
  isFuture ? 'future' :
  isToday  ? `today, ${isHit ? 'done' : 'not yet done'}` :
             (isHit ? 'done' : 'not done')
}`}
```

---

### `src/components/dashboard-shell.tsx` — component (state hoist), event-driven

**Analog:** NONE in repo (first React 19 `useOptimistic` site). Follow 02-RESEARCH.md Pattern 4.

**Canonical code** (02-RESEARCH.md lines 529-590):

```typescript
'use client'

import { useOptimistic, startTransition, useState } from 'react'
import { toast } from 'sonner'
import { GoalCard } from '@/components/goal-card'
import { incrementCountAction, /* ... */ } from '@/server/actions/progress'

type Action =
  | { type: 'count:increment';  goalId: string; delta: number }
  | { type: 'checklist:toggle'; goalId: string; taskId: string; isDone: boolean }
  | { type: 'habit:toggle';     goalId: string; localDate: string; isChecked: boolean }

function reducer(current: Goal[], action: Action): Goal[] {
  // Exhaustive switch on action.type; TS discriminated union guarantees compile-time coverage.
}

export function DashboardShell({ initialGoals, userTz, now }: Props) {
  const [goals, dispatch] = useOptimistic(initialGoals, reducer)

  async function handleIncrement(goalId: string, delta: number) {
    const undoId = crypto.randomUUID()
    startTransition(async () => {
      dispatch({ type: 'count:increment', goalId, delta })
      const result = await incrementCountAction({ goalId, delta, undoId })
      if (!result.ok) {
        toast.error(result.error ?? 'Could not save — undo in place')
        return
      }
      toast('Logged +' + delta, {
        id: 'progress-undo',     // D-34: most-recent-only (single id across types)
        action: {
          label: 'Undo',
          onClick: () => {
            startTransition(async () => {
              dispatch({ type: 'count:increment', goalId, delta: -delta })
              await undoLastMutationAction({ undoId })
            })
          },
        },
        duration: 6000,           // D-32: 6-second window
      })
    })
  }

  return <>{goals.map(g => <GoalCard key={g.id} goal={g} ... />)}</>
}
```

**Critical invariants** (02-RESEARCH.md lines 592-595):
- `startTransition` wraps BOTH `dispatch` AND the `await` — dispatching outside a transition throws in React 19
- Client generates `undoId` via `crypto.randomUUID()`; server action receives + stores it so undo targets that specific mutation (D-34)
- Single `id: 'progress-undo'` on every toast call — Sonner replaces rather than stacks (D-34)
- On server `{ok:false}` — do NOT roll back optimistic state manually; `useOptimistic` rolls back on next RSC revalidation automatically

---

### `src/components/goal-card/index.tsx` — component (discriminator), transform

**Analog:** `src/components/ui/button.tsx` (lines 7-39 — `cva` variant pattern), but applied at the component level rather than className level.

**Apply to Phase 2:** Switch on `goal.type` and render the corresponding variant:

```typescript
import { CountCard } from './count'
import { ChecklistCard } from './checklist'
import { HabitCard } from './habit'

export function GoalCard({ goal, ...props }: GoalCardProps) {
  switch (goal.type) {
    case 'count':     return <CountCard goal={goal} {...props} />
    case 'checklist': return <ChecklistCard goal={goal} {...props} />
    case 'habit':     return <HabitCard goal={goal} {...props} />
    // TS discriminated union → exhaustiveness check with: const _exhaustive: never = goal
  }
}
```

---

### `src/components/goal-card/{count,checklist,habit}.tsx` — component, event-driven

**Analog:** `src/components/auth/login-form.tsx` (lines 1-117) — client component with Server Action call + error surface pattern.

**`"use client"` + imports pattern** (lines 1-20):

```typescript
"use client"

import { useForm } from "react-hook-form"
import { zodResolver } from "@hookform/resolvers/zod"
import { Loader2 } from "lucide-react"
import Link from "next/link"
import { signInAction } from "@/server/actions/auth"
import { signInSchema, type SignInInput } from "@/lib/schemas/auth"
import { Button } from "@/components/ui/button"
import { Input } from "@/components/ui/input"
// ... etc
```

**Apply to Phase 2:**
- Replace auth imports with `{ incrementCountAction, toggleTaskAction, upsertHabitCheckInAction }` etc. from `@/server/actions/progress`
- Cards do NOT own `useOptimistic` — they call handlers passed from `DashboardShell` (02-RESEARCH.md Pattern 4)
- Inner UI (per-type surface) follows 02-UI-SPEC.md anatomy (count: `+1` button + stepper at lines 469-489; checklist: task rows at lines 492-514; habit: `<HabitGrid />` at lines 516-552)

**Header anatomy (shared)** — every card variant MUST wrap with this shell per 02-UI-SPEC.md lines 437-451:

```tsx
<Card>
  <CardHeader className="flex flex-row items-start justify-between">
    <CardTitle className="flex items-center gap-2 text-sm font-semibold">
      <TypeIcon />  {/* Target | ListChecks | Flame */}
      {goal.title}
    </CardTitle>
    <DropdownMenu>{/* kebab */}</DropdownMenu>
  </CardHeader>
  <CardContent className="flex flex-col gap-4">
    <div className="flex items-center gap-2">
      <ProgressBar {...} className="flex-1" />
      <PaceChip {...} />
    </div>
    {/* type-specific surface */}
    <p className="text-xs text-muted-foreground tabular-nums text-right">
      {rawCountLabel}
    </p>
  </CardContent>
</Card>
```

---

### `src/components/create-goal-dialog.tsx` — component (form), request-response

**Analog:** `src/components/auth/signup-form.tsx` (lines 1-167)

**RHF + zodResolver + Form primitives pattern** (lines 33-52):

```typescript
type SignUpFormValues = z.input<typeof signUpSchema>

export function SignUpForm() {
  const form = useForm<SignUpFormValues>({
    resolver: zodResolver(signUpSchema),
    defaultValues: {
      email: "",
      password: "",
      confirmPassword: "",
      timezone: "UTC",
    },
  })

  const {
    handleSubmit,
    setError,
    reset,
    formState: { errors, isSubmitting },
  } = form
```

**Zod 4 input-vs-output comment block** (lines 24-31) — COPY this docstring verbatim when the create-goal schema has a `.default(...)` or `.optional()` field:

```typescript
/**
 * signUpSchema uses `z.string().default("UTC")` for timezone, which makes the
 * Zod INPUT type differ from the OUTPUT type. @hookform/resolvers types the
 * Resolver using the INPUT shape, so RHF's form values must mirror that shape.
 * We use z.input here and pass the parsed SignUpInput (output) to the server
 * action in onSubmit.
 */
type SignUpFormValues = z.input<typeof signUpSchema>
```

**onSubmit → action → setError pattern** (lines 53-69):

```typescript
async function onSubmit(data: SignUpFormValues) {
  // ... transform if needed
  const result = await signUpAction(payload)
  if (result?.error) {
    setError("root", { message: result.error })
    return
  }
  reset()
  setSuccess(true)
}
```

**Form shell pattern** (lines 82-156):

```typescript
return (
  <Form {...form}>
    <form onSubmit={handleSubmit(onSubmit)} className="space-y-4" aria-busy={isSubmitting}>
      <FormField
        control={form.control}
        name="email"
        render={({ field }) => (
          <FormItem>
            <FormLabel>Email</FormLabel>
            <FormControl>
              <Input type="email" placeholder="you@example.com" {...field} />
            </FormControl>
            <FormMessage />
          </FormItem>
        )}
      />
      {/* ... more fields ... */}
      {errors.root?.message && (
        <div aria-live="polite">
          <Alert variant="destructive">
            <AlertDescription>{errors.root.message}</AlertDescription>
          </Alert>
        </div>
      )}
      <Button type="submit" disabled={isSubmitting}>
        {isSubmitting ? <Loader2 className="h-4 w-4 animate-spin" /> : "Create account"}
      </Button>
    </form>
  </Form>
)
```

**Apply to Phase 2:**
- 2-step flow = two render branches inside the same `<DialogContent>`. Step 1 = goal-type-picker (3 big `<button>` cards); Step 2 = per-type fields resolved from discriminated-union schema. Use `useState<'step1' | 'step2'>`
- Wrap form in shadcn `<Dialog>`/`<DialogContent>`/`<DialogHeader>` (install per 02-UI-SPEC.md line 56: `npx shadcn@latest add dialog`)
- Per-type field rendering: conditional on `form.watch('type')` — `count` → `targetCount` input; `checklist` → task list (see 02-UI-SPEC.md lines 310-315); `habit` → `targetDays` input
- Follow 02-UI-SPEC.md §Copywriting Contract Create Dialog (lines 267-325) for exact labels / placeholders / button text

---

### `src/components/delete-goal-dialog.tsx` — component (form), request-response

**Analog:** `src/components/auth/reset-password-form.tsx` (lines 1-103) — simpler single-action confirm dialog shape

**Pattern to copy** (success-state branching lines 22-54):

```typescript
const [sent, setSent] = React.useState(false)

async function onSubmit(data: ResetRequestInput) {
  const result = await requestPasswordResetAction(data)
  if (result?.error) {
    setError("root", { message: result.error })
    return
  }
  setSent(true)
}
```

**Apply to Phase 2:**
- Wrap in shadcn `<AlertDialog>` (install per 02-UI-SPEC.md line 56: `npx shadcn@latest add alert-dialog`)
- Use `variant="destructive"` from `button.tsx` (already exists — lines 13-14 of `src/components/ui/button.tsx`)
- Copy exact strings from 02-UI-SPEC.md lines 338-345:
  - Title: `Delete {goal title}?`
  - Description: `This removes the goal and all its progress. This can't be undone.`
  - Cancel button: `Cancel`
  - Confirm button: `Delete goal` with `variant="destructive"`
- Default focus on Cancel (shadcn's `AlertDialogCancel` handles this automatically)

---

### `src/components/earlier-day-popover.tsx` — component (form), request-response

**Analog:** `src/components/auth/update-password-form.tsx` (lines 1-103)

**RHF + action + error pattern** (identical to update-password-form):

```typescript
const form = useForm<UpdatePasswordInput>({
  resolver: zodResolver(updatePasswordSchema),
  defaultValues: { password: "", confirmPassword: "" },
})

async function onSubmit(data: UpdatePasswordInput) {
  const result = await updatePasswordAction(data)
  if (result?.error) {
    setError("root", { message: result.error })
  }
}
```

**Apply to Phase 2:**
- Wrap in shadcn `<Popover>` / `<PopoverContent>` (install per 02-UI-SPEC.md: `npx shadcn@latest add popover`)
- Form has two fields: `date` (constrained to current month, past days only — reuse `<HabitGrid>` builder per 02-UI-SPEC.md line 598) + `amount` (stepper, defaults to 1)
- Schema = `backfillCountSchema` from `src/lib/schemas/goals.ts` — pattern mirrors `updatePasswordSchema` with a custom refinement that `date` is strictly before today in userTz
- On success → close popover → `dashboard-shell.tsx` fires backfill toast (re-uses the same `id:'progress-undo'` toast slot)

---

### `src/components/empty-state.tsx` — component (RSC-safe), request-response

**Analog:** `src/app/page.tsx` (lines 6-38) — RSC with inline `<main>` + `<header>` layout

**Pattern to copy** (lines 18-37):

```tsx
<div className="min-h-screen flex flex-col">
  <header className="border-b border-border px-6 py-4 flex items-center justify-between">
    <p className="text-sm text-muted-foreground">Commitment Tracker</p>
    <form action={signOutAction}>
      <Button type="submit" variant="outline" size="sm">Log out</Button>
    </form>
  </header>
  <main className="flex flex-1 flex-col items-center justify-center px-4">
    <div className="text-center space-y-2">
      <h1 className="text-2xl font-semibold">Welcome, {user.email}</h1>
      <p className="text-base text-muted-foreground">Your goals are coming in Phase 2.</p>
    </div>
  </main>
</div>
```

**Apply to Phase 2:** Copy exact copy from 02-UI-SPEC.md §Empty State lines 244-254:
- Headline: `It's {Month Year}. What do you want to commit to?`
- Three illustrative example cards (hardcoded non-clickable — `opacity-50 pointer-events-none`)
- CTA button: `Add your first goal` — opens create dialog (via client child button — the trigger must be a client component even though empty-state itself can be RSC)

---

### `src/components/goal-type-picker.tsx` — component, event-driven (internal to create-goal-dialog)

**Analog:** Client-state pattern from `src/components/auth/login-form.tsx` (`React.useState` + event dispatch — lines 32-41).

**Apply to Phase 2:** Three buttons rendered inside Step 1 of `create-goal-dialog.tsx`. Clicking one calls a parent callback `onTypeSelected(type: 'count' | 'checklist' | 'habit')` which advances the dialog to Step 2. Copy/semantics per 02-UI-SPEC.md lines 271-281.

---

## Shared Patterns

### Pattern: Server Action shape

**Source:** `src/server/actions/auth.ts` (lines 28-74, `signUpAction`)
**Apply to:** Every new action in `src/server/actions/goals.ts` and `src/server/actions/progress.ts`

1. `"use server"` directive at top of file (line 1)
2. Zod `safeParse(input)` FIRST — return error before any Supabase / DB call (lines 31-34)
3. `await getSupabaseServerClient()` → `supabase.auth.getUser()` (lines 37, plus `src/lib/supabase/server.ts` contract)
4. Service / DB mutation, wrapped in `db.transaction()` if multi-write (D-06 / D-33 atomicity)
5. `revalidatePath('/dashboard')` after mutation (02-RESEARCH.md Pattern 5)
6. Return `{ ok: true, data }` / `{ ok: false, error }` (02-RESEARCH.md Pattern 5 — richer than auth.ts's `{error?}` shape because Pattern 4's `useOptimistic` narrowing needs `result.ok`)

---

### Pattern: RHF + Zod + Server Action form

**Source:** `src/components/auth/login-form.tsx` (full file — lines 1-117)
**Apply to:** `create-goal-dialog.tsx`, `delete-goal-dialog.tsx`, `earlier-day-popover.tsx`, any form component

1. `"use client"` at top
2. `useForm<Input>({ resolver: zodResolver(schema), defaultValues: {...} })`
3. Destructure `handleSubmit, setError, formState: { errors, isSubmitting }`
4. `onSubmit` calls the server action; on `result.error` → `setError("root", { message: result.error })`
5. Wrap form in `<Form {...form}>`; each field in `<FormField>` + `<FormItem>` + `<FormLabel>` + `<FormControl>` + `<FormMessage>`
6. Submit button: `disabled={isSubmitting}`; loading state renders `<Loader2 className="h-4 w-4 animate-spin" />` from `lucide-react`
7. Root-error surface: `{errors.root?.message && <div aria-live="polite"><Alert variant="destructive">...</Alert></div>}`

---

### Pattern: RLS on child tables (ownership via JOIN)

**Source:** `src/server/db/schema.ts` (lines 73-93 — existing `goals` 4-policy set)
**Apply to:** `tasks`, `habit_check_ins`, `progress_entries`

Drizzle `pgPolicy()` with:
- `for: "select" | "insert" | "update" | "delete"`
- `to: authenticatedRole` (imported from `drizzle-orm/supabase`)
- `using`: `sql\`goal_id IN (SELECT id FROM goals WHERE user_id = auth.uid())\``
- `withCheck` (insert/update only): same predicate

Defense-in-depth: Server actions / services MUST ALSO assert ownership before mutating — do not rely solely on RLS (02-CONTEXT.md code-context line 203).

---

### Pattern: Pure function + Vitest fixtures

**Source:** `src/lib/time.ts` (lines 1-26) + `tests/time.test.ts` (lines 1-79)
**Apply to:** `src/lib/progress.ts` + `tests/progress.test.ts`

- Function takes deterministic inputs (no `new Date()` / no `Math.random()` inside)
- Tests use `new Date('YYYY-MM-DDTHH:mm:ss.sssZ')` fixtures (exact UTC ISO)
- Cover DST spring-forward (Mar 8, 2026), leap year (Feb 29, 2028), edge-of-month timestamps, and each type's (empty / on-pace / behind / ahead / complete) state

---

### Pattern: Migration workflow

**Source:** `supabase/migrations/0000_initial_schema.sql` (drizzle-kit auto-generated) + `supabase/migrations/0001_custom_constraints.sql` (hand-authored CHECK)
**Apply to:** Phase 2 new tables (drizzle-kit → 0002) + polymorphic CHECK (hand-authored → 0003)

1. Extend `src/server/db/schema.ts` in place
2. Run `drizzle-kit generate` → produces `supabase/migrations/0002_*.sql` automatically (includes new tables + RLS policies)
3. Hand-author `supabase/migrations/0003_polymorphic_check.sql` for the multi-column CHECK that Drizzle cannot express (follows `0001_custom_constraints.sql` header-comment pattern — lines 1-2: `-- CHECK constraint: ... (GOAL-04, CONTEXT.md D-XX) -- Cannot be expressed in Drizzle pgTable; added manually per RESEARCH.md Pitfall 3.`)
4. Apply via `supabase db push --linked` (CONTEXT.md D-09)
5. Verify with inline `node -e` + `postgres.js` script (Plan 01-03 pattern — see STATE.md for exact verification script)

---

### Pattern: Defense-in-depth auth gate

**Source:** `src/app/page.tsx` (lines 7-16)
**Apply to:** `src/app/(protected)/dashboard/page.tsx`

```typescript
const supabase = await getSupabaseServerClient()
const { data: { user } } = await supabase.auth.getUser()
if (!user) redirect("/login")
```

Middleware already redirects, but this belt-and-suspenders pattern ensures RSCs fail closed (the comment at `src/app/page.tsx` line 13 documents exactly this rationale).

---

### Pattern: Token usage in Tailwind v4

**Source:** `src/app/globals.css` (lines 4-35 light mode + lines 38-66 dark mode)
**Apply to:** All Phase 2 components AND the `@theme` extension block for pace-chip tokens

Rules (STATE.md Plan 01-05 lesson):
1. Add new tokens INSIDE the existing `@theme { ... }` block — do NOT create a second `@theme` block
2. Do NOT override `--spacing-*` / `--radius-*` — they shadow Tailwind v4 core utilities
3. Add matching entries inside `@media (prefers-color-scheme: dark) { @theme { ... } }` for dark mode
4. Name new tokens `--color-warning-*` / `--color-success-*` — do NOT reuse `--color-destructive-*` for the pace chip (pace-behind is NOT an error — PITFALLS §1)
5. Components consume tokens via Tailwind utilities (`bg-warning-muted`, `text-success-foreground`, etc.) — do NOT hard-code `oklch()` values

---

### Pattern: Existing shadcn primitives in repo

**Source:** `src/components/ui/{card,button,input,label,alert,form}.tsx`
**Apply to:** Every Phase 2 component — reuse without modification

- Card shell: `<Card>` + `<CardHeader>` + `<CardTitle>` + `<CardContent>` from `src/components/ui/card.tsx` (lines 5-92)
- Buttons: `<Button variant="default|destructive|outline|ghost|link" size="default|sm|lg|icon">` from `src/components/ui/button.tsx` (lines 7-39)
- Alerts: `<Alert variant="default|destructive">` + `<AlertDescription>` from `src/components/ui/alert.tsx` (lines 6-66)
- Form: `<Form>`, `<FormField>`, `<FormItem>`, `<FormLabel>`, `<FormControl>`, `<FormMessage>` — used identically to login-form.tsx lines 43-93

**To install** (one-time, Wave-0 per 02-UI-SPEC.md line 56):

```bash
npx shadcn@latest add dialog alert-dialog dropdown-menu checkbox popover sonner
```

---

### Pattern: Toaster mount — EXACTLY ONCE in root layout

**Source:** 02-RESEARCH.md Pitfall 6 + 02-UI-SPEC.md line 70
**Apply to:** `src/app/layout.tsx` (modified — current shape at lines 13-19)

Add `<Toaster />` imported from `@/components/ui/sonner` INSIDE `<body>`, OUTSIDE `{children}`:

```tsx
<body className="bg-background text-foreground antialiased">
  {children}
  <Toaster />
</body>
```

Do NOT put Toaster inside a route subtree — toasts fire silently from descendants outside the provider subtree (02-RESEARCH.md Pitfall 6).

---

## No Analog Found

Three files have no meaningful in-repo analog — they're first-of-their-kind in this codebase. These MUST follow 02-RESEARCH.md patterns verbatim rather than invent shapes.

| File | Role | Reason | Fallback Pattern Source |
|------|------|--------|-------------------------|
| `src/components/progress-bar.tsx` | component (animation) | First Motion component in repo; custom `scaleX` + `useReducedMotion` pattern is novel — addresses STATE.md CLS research flag | 02-RESEARCH.md Pattern 3 (lines 463-515) — canonical code with `initial={false}` + `transform-origin: left` invariants |
| `src/components/habit-grid.tsx` | component (grid) | First calendar-grid component; requires roving tabindex + `date-fns` `eachDayOfInterval` shape | 02-RESEARCH.md Pattern 6 + 02-UI-SPEC.md §Habit Card lines 516-552 |
| `src/components/dashboard-shell.tsx` | component (state hoist) | First `useOptimistic` + reducer site; novel `startTransition` + undo-id contract with server actions | 02-RESEARCH.md Pattern 4 (lines 523-590) — critical nuances at 592-595 |

---

## Metadata

**Analog search scope:**
- `src/server/db/**/*.ts` (2 files)
- `src/server/actions/**/*.ts` (1 file)
- `src/lib/**/*.ts` (3 files)
- `src/components/**/*.tsx` (11 files)
- `src/app/**/*.{ts,tsx}` (12 files)
- `supabase/migrations/**/*.sql` (2 files)
- `tests/**/*.ts` (5 files)

**Files scanned:** 36 source + 5 test + 2 migration = 43 total
**Pattern extraction date:** 2026-04-19
**Upstream sources:**
- 02-CONTEXT.md (40 locked decisions D-01..D-40)
- 02-RESEARCH.md (Patterns 1-6 + Pitfalls)
- 02-UI-SPEC.md (visual contract + component inventory)
